# ─────────────────────────────────────────────────────────────
# Variables de entorno del frontend (Vite)
# Copia este archivo como .env.local y completa los valores
# ─────────────────────────────────────────────────────────────

# URL base del backend Next.js.
# Dejar VACÍO en desarrollo para usar el proxy de Vite (/api → localhost:3001).
# En producción apuntar al dominio real: https://api.onemore.com
VITE_API_URL=
